import React,{useEffect,useMemo,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Home,Plus,Search,Settings,Terminal,Wifi,Power,Lightbulb,Monitor,Speaker,Gamepad2,Plug,Shield,SlidersHorizontal,Sparkles,Trash2} from 'lucide-react';
import './styles.css';

const TYPES={Light:Lightbulb,TV:Monitor,Audio:Speaker,Console:Gamepad2,Plug,Other:SlidersHorizontal};
const seed=[];
const nav=[['Inicio',Home],['Dispositivos',SlidersHorizontal],['Escenas',Sparkles]];

function App(){
 const [page,setPage]=useState('Inicio');
 const [devices,setDevices]=useState(()=>JSON.parse(localStorage.getItem('nexus-devices')||'null')||seed);
 const [dark,setDark]=useState(()=>localStorage.getItem('nexus-theme')!=='light');
 const [query,setQuery]=useState('');
 const [toast,setToast]=useState('');
 const [showAdd,setShowAdd]=useState(false);
 const [terminal,setTerminal]=useState(false);
 useEffect(()=>localStorage.setItem('nexus-devices',JSON.stringify(devices)),[devices]);
 useEffect(()=>localStorage.setItem('nexus-theme',dark?'dark':'light'),[dark]);
 useEffect(()=>{const f=e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();document.querySelector('.search input')?.focus()}if(e.key==='Escape'){setTerminal(false);setShowAdd(false)}};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[]);
 const notify=x=>{setToast(x);setTimeout(()=>setToast(''),2200)};
 const filtered=useMemo(()=>devices.filter(d=>(d.name+' '+d.type+' '+d.room).toLowerCase().includes(query.toLowerCase())),[devices,query]);
 const addDevice=d=>{setDevices(x=>[...x,{...d,id:crypto.randomUUID(),connected:false}]);setShowAdd(false);notify('Dispositivo guardado · falta conectar una integración real')};
 const toggle=id=>{setDevices(x=>x.map(d=>d.id===id?{...d,connected:!d.connected}:d));notify('Estado local actualizado · aún sin control físico')};
 const remove=id=>{setDevices(x=>x.filter(d=>d.id!==id));notify('Dispositivo eliminado')};
 return <div className={dark?'app':'app light'}><div className="aurora a1"/><div className="aurora a2"/>
 <aside className="sidebar"><div className="brand"><div className="brandMark">N</div><div><b>NEXUS</b><span>DEVICE CONTROL</span></div></div><nav>{nav.map(([n,I])=><button className={page===n?'active':''} onClick={()=>setPage(n)} key={n}><I size={18}/><span>{n}</span></button>)}</nav><div className="sideBottom"><button onClick={()=>setTerminal(true)}><Terminal size={18}/><span>Terminal</span></button><button onClick={()=>setPage('Ajustes')}><Settings size={18}/><span>Ajustes</span></button></div></aside>
 <main><header><div className="mobileBrand"><div className="brandMark">N</div><b>NEXUS</b></div><div className="search"><Search size={17}/><input placeholder="Buscar dispositivos..." value={query} onChange={e=>setQuery(e.target.value)}/><kbd>⌘ K</kbd></div><div className="headActions"><button className="iconBtn" onClick={()=>setTerminal(true)}><Terminal size={18}/></button><button className="avatar" onClick={()=>setDark(!dark)}>N</button></div></header>
 <section className="content"><div className="eyebrow"><span className="pulse"/> NEXUS LOCAL <span>•</span> {devices.length} DISPOSITIVOS CONFIGURADOS</div>
 <div className="hero"><div><h1>{page==='Inicio'?'Tu casa, conectada.':page}</h1><p>NEXUS será tu centro de control. Ahora puedes preparar tus dispositivos y escenas; cuando exista una integración real, los comandos podrán actuar sobre el hardware.</p><div className="heroBtns"><button className="primary" onClick={()=>setShowAdd(true)}><Plus size={17}/> Agregar dispositivo</button><button className="secondary" onClick={()=>setTerminal(true)}><Terminal size={17}/> Terminal</button></div></div><div className="orb"><div className="orbCore"><Wifi size={28}/></div></div></div>
 {page==='Inicio'&&<Dashboard devices={filtered} toggle={toggle} remove={remove} add={()=>setShowAdd(true)}/>} 
 {page==='Dispositivos'&&<DeviceList devices={filtered} toggle={toggle} remove={remove} add={()=>setShowAdd(true)}/>} 
 {page==='Escenas'&&<Scenes devices={devices} notify={notify}/>} 
 {page==='Ajustes'&&<SettingsPage dark={dark} setDark={setDark} clear={()=>{setDevices([]);notify('Configuración de dispositivos limpiada')}}/>}
 <footer><span>NEXUS v2.2 • Device Control</span><span><Shield size={13}/> Sin conexiones externas</span></footer></section></main>
 {toast&&<div className="toast"><span>✓</span>{toast}</div>}
 {showAdd&&<AddModal onClose={()=>setShowAdd(false)} onAdd={addDevice}/>} 
 {terminal&&<div className="modal" onClick={()=>setTerminal(false)}><div className="terminal" onClick={e=>e.stopPropagation()}><div className="termTop"><b>NEXUS TERMINAL</b><button onClick={()=>setTerminal(false)}>×</button></div><div className="termBody"><div>&gt; nexus status</div><div className="green">LOCAL UI READY</div><div>&gt; integration</div><div>Waiting for Home Assistant / Matter / device API</div><div>&gt; ready</div><div className="cursor">█</div></div></div></div>}
 </div>
}
function Dashboard({devices,toggle,remove,add}){const online=devices.filter(d=>d.connected).length;return <><div className="stats">{[['Dispositivos',devices.length,'Configurados'],['Conectados',online,'Control local pendiente'],['Habitaciones',new Set(devices.map(d=>d.room).filter(Boolean)).size,'Configuradas'],['Integraciones',0,'Sin conectar']].map(x=><div className="card stat" key={x[0]}><span>{x[0]}</span><strong>{x[1]}</strong><small className="ok">{x[2]}</small></div>)}</div><section className="panel"><div className="panelHead"><div><span className="eyebrow">DEVICE MATRIX</span><h2>Tus dispositivos</h2></div><button className="ghost" onClick={add}><Plus size={15}/> Agregar</button></div>{devices.length?<div className="deviceGrid">{devices.slice(0,6).map(d=><Card d={d} toggle={toggle} remove={remove} key={d.id}/>)}</div>:<Empty add={add}/>}</section></>}
function DeviceList({devices,toggle,remove,add}){return <section className="panel full"><div className="panelHead"><div><span className="eyebrow">DEVICE MATRIX</span><h2>Todos tus dispositivos</h2></div><button className="primary" onClick={add}><Plus size={15}/> Agregar</button></div>{devices.length?<div className="deviceGrid wide">{devices.map(d=><Card d={d} toggle={toggle} remove={remove} key={d.id}/>)}</div>:<Empty add={add}/>}</section>}
function Card({d,toggle,remove}){const I=TYPES[d.type]||SlidersHorizontal;return <div className="device card"><div className="deviceTop"><div className="deviceIcon"><I size={19}/></div><span className={d.connected?'online':'offline'}><i/> {d.connected?'Conectado':'Sin conexión'}</span></div><h3>{d.name}</h3><p>{d.type} • {d.room||'Sin habitación'}</p><div className="meter"><span style={{width:d.connected?'100%':'0%'}}/></div><div className="deviceBottom"><small>{d.integration||'Integración pendiente'}</small><div style={{display:'flex',gap:5}}><button onClick={()=>toggle(d.id)}><Power size={12}/> {d.connected?'Desconectar':'Preparar'}</button><button onClick={()=>remove(d.id)}><Trash2 size={12}/></button></div></div></div>}
function Empty({add}){return <div className="placeholder"><Wifi size={32}/><h3>Aún no hay dispositivos</h3><p>Agrega los dispositivos que quieres controlar. NEXUS no inventará estados ni conexiones.</p><button className="primary" onClick={add}><Plus size={15}/> Agregar dispositivo</button></div>}
function AddModal({onClose,onAdd}){const [name,setName]=useState('');const [type,setType]=useState('Light');const [room,setRoom]=useState('Sala');const [integration,setIntegration]=useState('Home Assistant');return <div className="modal" onClick={onClose}><div className="terminal" onClick={e=>e.stopPropagation()}><div className="termTop"><b>AGREGAR DISPOSITIVO</b><button onClick={onClose}>×</button></div><div className="termBody" style={{fontFamily:'Inter'}}><input className="formInput" placeholder="Nombre (ej. Luz sala)" value={name} onChange={e=>setName(e.target.value)}/><select className="formInput" value={type} onChange={e=>setType(e.target.value)}>{Object.keys(TYPES).map(x=><option key={x}>{x}</option>)}</select><input className="formInput" placeholder="Habitación" value={room} onChange={e=>setRoom(e.target.value)}/><select className="formInput" value={integration} onChange={e=>setIntegration(e.target.value)}><option>Home Assistant</option><option>Matter</option><option>API / Webhook</option><option>Pendiente</option></select><button className="primary" disabled={!name.trim()} onClick={()=>onAdd({name:name.trim(),type,room,integration})}>Guardar dispositivo</button></div></div></div>}
function Scenes({devices,notify}){const run=name=>notify(`${name}: escena preparada · requiere integración para ejecutar`);return <section className="panel full"><div className="panelHead"><div><span className="eyebrow">SCENES</span><h2>Escenas de casa</h2></div><Sparkles size={21}/></div><p className="moduleDesc">Crea acciones para varios dispositivos. La ejecución física se habilitará al conectar una integración.</p><div className="sceneGrid"><button className="scene card" onClick={()=>run('Modo cine')}><Monitor size={19}/><b>Modo cine</b><span>TV + audio + luces</span></button><button className="scene card" onClick={()=>run('Modo gaming')}><Gamepad2 size={19}/><b>Modo gaming</b><span>Consola + TV + luces</span></button><button className="scene card" onClick={()=>run('Apagar todo')}><Power size={19}/><b>Apagar todo</b><span>Todos los dispositivos</span></button></div></section>}
function SettingsPage({dark,setDark,clear}){return <section className="panel full"><div className="panelHead"><div><span className="eyebrow">PREFERENCES</span><h2>Ajustes</h2></div><Settings size={21}/></div><div className="settingsList"><label><span>Modo oscuro</span><input type="checkbox" checked={dark} onChange={e=>setDark(e.target.checked)}/></label><label><span>Arquitectura local-first</span><b>Activa</b></label><label><span>Integración física</span><b>Pendiente</b></label></div><button className="danger" onClick={clear}>Limpiar dispositivos guardados</button></section>}
createRoot(document.getElementById('root')).render(<App/>);
